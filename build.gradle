// Apply plugins
plugins {
    id 'com.android.application' version '8.9.0' apply false
    id 'com.google.gms.google-services' version '4.4.0' apply false

}

// Remove repositories block! ❌

// Clean Task
task clean(type: Delete) {
    delete rootProject.buildDir
}